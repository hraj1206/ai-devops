services:
  - type: web_service
    name: ai-devops-groq-backend
    env: docker
    plan: free
    branch: main
    root: ./
    dockerfilePath: Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false
    autoDeploy: true
